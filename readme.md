# netscape-link-extractor

A simple program that extracts links from html files using regex and can output the links in many formats.

## usage

use `--help` to learn how to use the program.

## build

clone the repo and run `cargo build`.
